server:
  port: 8082

spring:
  application:
    name: instance-service
  datasource:
    url: ${INSTANCE_DB_URL:jdbc:mysql://localhost:3306/sentinal_instance?createDatabaseIfNotExist=true}
    username: ${INSTANCE_DB_USERNAME:root}
    password: ${INSTANCE_DB_PASSWORD:ritesh85}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect